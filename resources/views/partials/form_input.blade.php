<div class="form_div">
    <input type="{{ $type ?? 'text' }}" class="form_input" name="{{ $name }}" placeholder=" " value="{{ old($name) }}">
</div>
<label class="form_label"><b>{{ $label }}</b></label>
